memgraph:
  # Native HA deployment starts from tests/stress; this path resolves to build/query_modules.
  args:
    - "--query-modules-directory=../../build/query_modules"

customWorkloads:
  tests:
    - name: vector-index-concurrent-ops
      script: "ha/workloads/vector_index_concurrent_ops/workload.py"
      timeout_min: 10
