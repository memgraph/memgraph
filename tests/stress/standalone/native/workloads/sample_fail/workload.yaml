memgraph:
  deployment:
    externally_managed: false
  args: []

general:
  verbose: false
  use_ssl: false

dataset:
  tests: []

customWorkloads:
  tests:
    - name: sample-workload-fail
      memgraph_args: []
      # This workload intentionally fails to test error handling
      script: "standalone/native/workloads/sample_fail/workload.py"
      timeout_min: 5
