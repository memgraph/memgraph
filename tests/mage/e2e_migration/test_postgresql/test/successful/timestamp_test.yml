query: |
  CALL migrate.postgresql('SELECT timestamp_col FROM dummy_table', {
      host: 'postgresql_test',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.timestamp_col) AS timestamp_col_type, toString(row.timestamp_col) AS timestamp_col

output:
  - timestamp_col_type: 'LOCAL_DATE_TIME'
    timestamp_col: '2023-12-25T10:30:45.000000'
