query: |
  CALL migrate.postgresql('SELECT char_col FROM dummy_table', {
      host: 'postgresql_test',
      port: 5432,
      user: 'memgraph',
      password: 'memgraph_password',
      database: 'test_db'
  })
  YIELD row
  RETURN valueType(row.char_col) AS char_col_type, row.char_col AS char_col

output:
  - char_col_type: 'STRING'
    char_col: 'char_test '
