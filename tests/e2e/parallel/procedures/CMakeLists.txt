# Procedures for parallel execution tests
# These are copied to the build directory for loading during tests
copy_parallel_e2e_python_files(read_proc.py)
copy_parallel_e2e_python_files(write_proc.py)
