query: >
  MATCH p = (a:Student {name: 'Ana'})-[:FRIEND*]->(c)
  WITH collect(p) AS paths
  CALL convert.to_tree(paths) YIELD value
  RETURN value

output:
  - value:
      name: Ana
      FRIEND:
        - name: Bob
          FRIEND:
            - name: Carol
