# PR Review Issue Table

Source: deduped and expanded from `review_notes.txt`, then updated after:

- `323349340` `Decouple storage config from DB arena runtime`
- `ed9177acd` `Default storage config equality`

Legend:

- **Status**: Open, In progress, Resolved, Needs verification.
- **Type**: Bug risk, implementation request, design decision, verification, docs/cleanup, tests.
- **Difficulty**: S = local/simple, M = several files or careful audit, L = cross-cutting/high-risk.
- **Priority**: P0 = production blocker, P1 = should fix before merge, P2 = polish/follow-up candidate.

## Recently Closed / Changed

| ID | Area | Type | Status | What changed | Verification / notes | Difficulty | Priority |
|---|---|---|---|---|---|---|---|
| DONE-01 | Storage config | Implementation request | Resolved | Removed runtime-only `arena_registration` and `db_embedding_memory_tracker` from `storage::Config`. Runtime arena/tracker state is now passed through `Database`/`Storage` construction instead of being persisted as logical config. | Fixed in `323349340`. This also removed the original `flags/general.cpp` and `query/jsonl/reader.cppm` dependency-scan failures caused by `storage/v2/config.hpp` including jemalloc-facing headers. | M | P0 |
| DONE-02 | Storage config equality | Cleanup | Resolved | Reverted `Config` equality to a defaulted comparison after runtime fields were removed. | Fixed in `ed9177acd`; `mg-storage-v2` target builds after the change. | S | P2 |
| DONE-03 | Jemalloc include leakage | Implementation request | Mostly resolved | Split lightweight arena/runtime APIs into `memory/db_arena_fwd.hpp`; broad storage/query/integration headers no longer include `memory/db_arena.hpp`. `DbArena` ownership in `Database` is opaque in `database.hpp`. | Fixed for known build failures in `323349340`. Heavy jemalloc include remains intentionally in `memory/db_arena.hpp` and memory implementation files. | M | P1 |
| DONE-04 | Manual jemalloc pinning at call sites | Cleanup | Partially resolved | Removed direct `je_mallctl("thread.arena", ...)` from storage GC/snapshot, TTL, Kafka, Pulsar, and after-commit trigger initialization paths. | `323349340` replaced those with TLS scope/setup. Remaining design question: whether DB-aware threads should also bind jemalloc `thread.arena` centrally, not just TLS. | M | P0 |
| DONE-05 | DB arena lifecycle | Bug risk | Resolved | Serialized `DbArena::AcquireThreadArena()` cold path so `arena_handles_` and `thread_arena_map_` are updated under the same DB arena lock. | Local change; `memgraph__unit__db_memory_tracking` builds and `./build/tests/unit/db_memory_tracking` passes. | M | P0 |
| DONE-06 | DB arena lifecycle | Bug risk | Resolved | Per-thread arena acquisition now installs hooks on a temporary handle and publishes only after hook installation succeeds. It reserves publication storage before installing hooks and erases the placeholder map entry on install failure. | Local change; keeps failed arenas out of `arena_handles_` and avoids returning custom-hooked arenas to the reusable pool before destructor restoration. | M | P0 |
| DONE-07 | DB arena scope | Bug risk | Resolved | Added the non-jemalloc definition for `DbArenaScope(Database*)`, matching the unconditional declaration in `db_arena_fwd.hpp`. | Local change; jemalloc build target still compiles. Non-jemalloc build still needs a full configure/build pass later. | S | P1 |

## Open / Needs Verification

| ID | Area | Type | Status | Issue / question to resolve | Evidence / current state | Desired outcome | Difficulty | Priority |
|---|---|---|---|---|---|---|---|---|
| DBMEM-03 | DB arena lifecycle | Verification | Open | Per-thread arena mapping keys use `std::thread::id`; OS/runtime may reuse ids, and stale entries are not removed for short-lived DB-aware threads. | Some threads are long-lived, but streams/schema/recovery/worker tasks need audit. | Decide whether id reuse is harmless for all mapped threads. If not, add thread-exit cleanup or use a stronger registration lifetime. | L | P1 |
| DBMEM-04 | DB arena scope | Implementation request | Open | `DbArenaScope` should probably forbid nested scoping instead of saving/restoring arbitrary previous arena values. | Current scope supports nesting by saving previous TLS. Review request asks for debug assertion that TLS is clean at construction and reset at destruction. | Either enforce non-nesting with `DMG_ASSERT`/`MG_ASSERT`, or document the required nested cases and keep current behavior intentionally. | M | P1 |
| DBMEM-06 | DB-aware execution threads | Implementation request | Resolved | All long-lived DB-owned threads now use AcquireThreadArena(): GC runner (storage.cpp:482), after-commit trigger pool (database.cpp:81 via ThreadInitFn), TTL scheduler (SetAcquireArenaFn injected from InMemoryStorage), AsyncIndexer (SetAcquireArenaFn injected from InMemoryStorage). Replication client uses BaseArenaIdx() for short-lived thread-pool task dispatches — correct. Stream consumers handled via factory pattern (DBMEM-21/DBMEM-45). | Completed across multiple commits. | L | P0 |
| DBMEM-07 | DB-aware execution threads | Design decision | Resolved | TLS-only policy documented in `memory/db_arena_fwd.hpp` above `DbArenaScope`. Policy: arena attribution is TLS-based via `DbArenaScope`; raw `je_mallctl` thread.arena pinning is NOT used (restoring to per-CPU arenas returns EPERM). Three thread categories: long-lived dedicated threads use AcquireThreadArena() + SetAcquireArenaFn injection, short-lived pool tasks use BaseArenaIdx() scope, query threads set scope once DB is known. | Policy comment added to db_arena_fwd.hpp. | M | P0 |
| DBMEM-08 | Arena index semantics | Cleanup | Open | `ArenaIdx()` / `arena_idx` names are ambiguous after moving from one arena per DB to a per-DB arena pool. | Some uses mean base arena for allocator construction, others mean per-thread acquired arena, and some integration fields just carry DB execution context. Long-lived DB-owned threads that should call `AcquireThreadArena()` but currently use the base arena: `ttl.cpp:184`, `async_indexer.cpp:68`, `replication_client.cpp:221/265/289/301`, trigger pool (`database.cpp:106`, being fixed), stream factory (`sources.cpp`, see DBMEM-45). Short-lived shared-thread scopes (plan cache, replication handlers) are fine with base arena. | Rename APIs/fields to distinguish `BaseArenaIdx`, `AcquireThreadArena`, and DB execution context. Fix long-lived thread sites to call `AcquireThreadArena()`. Remove misleading "pool index" terminology where an actual jemalloc arena index is passed. | L | P1 |
| DBMEM-09 | Allocator strategy | Implementation request | Needs decision | `ArenaAwareAllocator` (stateful, captures base arena_idx at construction) vs `DbAwareAllocator` (stateless, reads TLS per allocation). **Recommendation: keep both, use each where correct.** `ArenaAwareAllocator` is safer for long-lived storage structures (vertices_, edges_, indices) that outlive any single thread scope — they always allocate to the right arena even if TLS is unset. `DbAwareAllocator` is correct for transient query/trigger allocations within a known DB scope. Full migration to TLS-only is high-risk: GC and background threads that deallocate query-allocated structures must have TLS correctly set, which is now true, but any future path that deallocates without TLS would silently mis-attribute. The reviewer's concern about contention benefits applies only if base-arena concentration actually causes measured contention — defer that migration until profiling shows it is needed. | Pending explicit team sign-off on two-allocator model vs full TLS migration. | L | P0 |
| DBMEM-10 | Allocator strategy | Bug risk | Open | Stateful `ArenaAwareAllocator` pins many long-lived structures to a base arena, which may undermine the per-thread arena pool's contention benefits. | Vertices, edges, labels, constraints, triggers, and indices still use stateful allocator patterns in many places. | Either route long-lived storage allocations through the active DB/thread arena or explicitly accept base-arena concentration for those structures. | L | P1 |
| DBMEM-11 | Allocator API/debugging | Verification | Open | `DbAwareAllocator` API and debug checks need a focused audit. | Review note asks for a list of functions/callers and simple debugging logic ensuring allocations/deallocations occur on correct arenas. | Produce a call map for `DbAllocateBytes`, `DbDeallocateBytes`, `DbAwareAllocator`, `ArenaAwareAllocator`, and deleters. Add simple debug checks if they can validate DB ownership without breaking cross-thread frees. | M | P1 |
| DBMEM-12 | Allocator debug checks | Bug risk | Open | Debug deallocation verification that compares pointer arena to current TLS arena may conflict with legitimate cross-thread frees. | GC and background cleanup can deallocate memory allocated by query/storage threads. | Validate "belongs to this DB arena pool" instead of "equals current TLS arena", or disable strict check on known cross-thread deallocation paths. | M | P1 |
| DBMEM-13 | Jemalloc encapsulation | Cleanup | Needs verification | Known production include leaks were fixed, but we should still verify there are no non-memory modules including `jemalloc/jemalloc.h`. | Current intended direct users should be memory implementation and, only if unavoidable, narrow owner `.cpp` files. | Run an include sweep before merge and keep jemalloc details behind `memory/db_arena.*` helpers. | S | P1 |
| DBMEM-14 | Hook behavior | Verification | Open | Confirm `db_arena.cpp` hooks still match master semantics except for associating a DB tracker with DB arenas. | Review asks whether `DbArenaHooks global_graph_arena_hooks{}` still behaves like old logic and whether hooks are restored before arena reuse. | Diff against master hook logic; document intentional deltas; add regression tests for alloc/commit/decommit/purge/restore paths. | M | P1 |
| DBMEM-15 | Arena pool / global arenas | Verification | Open | Confirm DB-specific arena pools do not replace global jemalloc arenas needed by non-DB allocations. | Jemalloc can create arenas per CPU; DB arenas are additional explicit arenas. Review asks whether global arenas are still configured as before. | Document global vs DB arena roles and verify startup/global memory-control behavior still creates/configures normal arenas. | M | P1 |
| DBMEM-16 | Query arena guard placement | Verification | Resolved | DbArenaScope added to Interpreter::Commit() data-tx path and Interpreter::Abort(); both guarded by #if USE_JEMALLOC. System-tx path returns early before Commit() scope. Abort() guarded by db_acc_ null check. PullPlan::Pull already had scope at line 3038. | Committed in "Scope query Commit/Abort and TTL scheduler to DB arena". | L | P0 |
| DBMEM-17 | ExecutionContext arena field | Design decision | Open | `ExecutionContext` may not need `db_arena_idx` if a scoped guard or thread/task wrapper handles arena state. | Current context carries an arena index for cross-thread/parallel work. | Decide whether this belongs in execution context or a lower-level query/session/task scope. | M | P2 |
| DBMEM-18 | Cross-thread query tracking | Verification | Open | `CrossThreadMemoryTracking` needs audit for query tracker, user resource tracker, DB arena behavior, and worker-pool task stealing. | Review calls out stolen tasks and symmetry of start/stop. | Verify start/stop symmetry, exception paths, task stealing, and scope restoration. | M | P1 |
| DBMEM-19 | Query memory accounting | Implementation request | Paused | Memory tracking should update on every 1 KiB delta. | Review request still needs code audit in memory tracker/query memory control. | Find current batching threshold, change to 1 KiB, and adjust tests. | S | P1 |
| DBMEM-20 | Query allocator construction | Verification | Open | `QueryAllocator execution_memory{db_acc->DbQueryMemoryTracker()}` needs checking in all construction paths. | Review note calls out correctness for target DBs and system queries. | Audit every `QueryAllocator`/`ThreadSafeQueryAllocator` construction and ensure DB tracker selection is correct. | M | P1 |
| DBMEM-45 | Stream consumer thread factory | Verification | Open | `MakeDbAwareThreadFactory` in `sources.cpp` captures a raw `unsigned arena_idx` (the base arena from `SetArenaIdx(ArenaIdx())`). The consumer thread never calls `AcquireThreadArena()`, so it shares the base arena instead of getting its own per-thread arena. Additionally the factory API (`ConsumerThreadFactory`) passes an `unsigned` not a `DbArena*`, so the stream layer structurally cannot fix this without an API change. | `sources.cpp:24-31`, `sources.cpp:49-51`, `sources.cpp:125-127`, `database.cpp:92`. | Change `SetArenaIdx(unsigned)` to accept a callable `std::function<unsigned()>` (or `ArenaRegistration*`) so the factory can call `AcquireThreadArena()` from the consumer thread on first run. Review whether consumer restart creates a second arena entry for the same thread. | M | P1 |
| DBMEM-21 | Streams / integrations | Implementation request | Resolved | Kafka/Pulsar consumers should not know about arenas directly. | `ConsumerInfo::arena_idx` and consumer-level arena setters were removed. Kafka/Pulsar consumers now take an optional generic thread factory; `query::stream` supplies a DB-aware factory that wraps the consumer loop in `DbArenaScope`. | Code change is local and verified by focused build plus `db_memory_tracking`; still needs commit and wider stream/integration tests. | M | P1 |
| DBMEM-22 | Triggers | Implementation request | Open | After-commit trigger pool should be DB-aware; before-commit triggers should inherit commit/query scope. | Direct pinning was replaced with TLS initializer task, but this is still a manual one-off path. Trigger stores also use arena-aware allocation. | Use DB-aware thread-pool/task wrapper; remove trigger-specific arena plumbing if commit/worker scopes cover it. | M | P1 |
| DBMEM-23 | Schema/index/constraint work | Verification | Resolved | AsyncIndexer thread (persistent DbAwareThread) now uses AcquireThreadArena() via injectable SetAcquireArenaFn() — same pattern as TTL. Injected from InMemoryStorage constructor. Replication client task dispatches use BaseArenaIdx() correctly (short-lived pool tasks, not long-lived threads). | Committed in "Scope AsyncIndexer thread to DB-acquired arena". | L | P0 |
| DBMEM-24 | GC / FREE MEMORY | Verification | Open | Background GC should be DB-aware, and `FREE MEMORY` query flow should also be scoped correctly. | Periodic GC now uses `DbArenaScope` locally, but the broader forced GC/FREE MEMORY flow is not audited. | Verify periodic GC, forced GC, rapid delta cleanup, and `FREE MEMORY` attribution/deallocation behavior. | L | P1 |
| DBMEM-25 | Durability recovery | Verification | Resolved | Subagent audit confirmed durability recovery paths already have correct DbArenaScope placement. No code changes needed. | Verified clean. | L | P0 |
| DBMEM-26 | WAL file ownership | Implementation request | Open | WAL file unique pointer/deleter logic is unnecessarily complex and split by jemalloc concerns. | Review calls out `InitializeWalFile` and split `unique_ptr`/deleter code. | Introduce a unified DB-aware deleter/helper that compiles cleanly in jemalloc and non-jemalloc builds. | M | P1 |
| DBMEM-27 | Delta container / PMR | Implementation request | Open | `delta_container` and page allocator code still expose arena-specific plumbing. | `ArenaMemoryResource` moved to a lightweight header, but `#if USE_JEMALLOC` and arena-specific resource choice remain in storage code. | Hide arena choice behind one allocator/memory-resource abstraction aligned with final allocator decision. | M | P1 |
| DBMEM-28 | Compressor / PropertyBuffer | Verification | Open | `utils/compressor` uses DB-aware allocation; confirm all compression/decompression happens with TLS set and buffers are destroyed under valid DB ownership. | Review notes this is mainly PropertyBuffer-related and needs careful flow audit. | Audit callers and lifetimes. If buffers can outlive DB scope or be freed cross-thread, use an allocator/deleter that records owning DB/pool safely. | M | P1 |
| DBMEM-29 | Vector index memory | Implementation request | Open | Vector index memory should use DB-specific embedding tracker whose parent is the global embedding tracker. | Runtime tracker is no longer stored in `Config`; it is passed from `Database` into storage/index construction. | Confirm all vector and edge-vector allocations charge `db_embedding_memory_tracker_` and roll up globally. | M | P1 |
| DBMEM-30 | Text index memory | Verification | Open | Text index memory tracking state is unclear. | Review asks for a current-state summary; text index is separate from regular indices and vector index. | Produce a short summary and decide whether text index memory belongs to DB storage, DB embedding, or another tracker. | S | P2 |
| DBMEM-31 | Tenant profiles durability/versioning | Verification | Open | Tenant profiles are durable and versioned, but migration/key semantics need review. | Review asks what KV store is used, whether `.database` durability can be reused, and how user resources do it. | Confirm version key naming, migration behavior, and storage namespace ownership. | M | P1 |
| DBMEM-32 | Tenant profiles data model | Verification | Open | Clarify profile/database relationship. Can one profile attach to many DBs, and can one DB attach to many profiles? | Existing model appears to allow many DBs per profile and one profile per DB. | Document invariants and add tests for moving DBs between profiles. | S | P1 |
| DBMEM-33 | Tenant profile durable mapping keys | Cleanup | Open | `db_tenant_profile:*` mapping keys may be durable dead data if they are written but not read on restart. | Review notes mapping keys should either be used or removed. | Remove unused durable mapping keys or intentionally read/validate them. | S | P2 |
| DBMEM-34 | Tenant profiles enterprise/license gating | Verification | Open | Enterprise `#ifdef MG_ENTERPRISE` and license behavior need systematic audit, including replication. | Review suggests query-level license check may be enough; replication may be allowed similarly to role/user-resource replication. | Confirm community build behavior, enterprise without license behavior, and replication semantics. | M | P1 |
| DBMEM-35 | Tenant profile RPC | Verification | Open | Tenant profile RPC request/response needs serialization, versioning, and failure semantics review. | Review calls out handler, timestamp checks, idempotency, and DB missing on replica. | Verify SLK type ids, backward compatibility, timestamp checks, idempotency, and missing-DB behavior. | M | P1 |
| DBMEM-36 | Tenant profile query prep / AST | Verification | Open | Tenant profile grammar, ANTLR definitions, visitor handling, clone functions, and interpreter preparation need review. | Review calls out Cypher main visitor handling, clone function, `PrepareTenantProfileQuery`, and system-query behavior. | Confirm parsing, AST clone, result shape, replica restrictions, parameter/value handling, overflow, and rollback behavior. | M | P1 |
| DBMEM-37 | Tenant profiles privileges | Design decision | Open | `Privilege::STATS` appears to gate tenant profile operations; verify whether this is correct. | Review asks whether `STATS` is new and whether mutating profile commands need stronger privilege. | Decide privilege model for create/alter/drop/set/remove/show tenant profile commands. | S | P1 |
| DBMEM-38 | Tenant profile system transaction | Verification | Open | Tenant profile queries are treated like system queries; durability and replication commit path need confirmation. | Preparation uses system transaction paths. | Verify system transaction lifetime, rollback on query failure, and consistency with role/user-resource replication. | M | P1 |
| DBMEM-39 | Replication storage client | Verification | Open | `ReplicationStorageClient` should be database/storage-specific; its thread-pool tasks should be DB-aware and not require per-transaction arena index fields. | `ReplicationTransaction` still carries arena index state. | Audit replication tasks and remove redundant arena index propagation if DB-aware storage/client ownership covers it. | L | P1 |
| DBMEM-40 | Info queries | Verification | Open | Changed info queries should be checked for correctness and simplification opportunities. | Review note asks specifically to verify changed info query logic. | Confirm values are correct, stable under tenant/profile changes, and not overcomplicated. | S | P2 |
| DBMEM-41 | Documentation / ADR | Docs/cleanup | Open | Remove standalone docs/scratch plans and move final design into an ADR matching existing format. | Review asks to remove docs dir and fold relevant info into an ADR. Existing examples live under `ADRs/`. | Create an ADR with author/status/date/problem/criteria/decision/consequences and delete stale planning docs. | M | P1 |
| DBMEM-42 | Comments | Docs/cleanup | Open | Many comments are misleading or obsolete, especially around decay, tcache, prompt freeing, and nested scope support. | Some comments were improved while fixing include leaks, but a full pass remains. | Preserve comments that explain invariants/restrictions; remove speculative review notes and stale implementation history. | M | P1 |
| DBMEM-43 | Generated docs/plans | Cleanup | Open | Branch-local notes/plans likely should not ship as production artifacts. | Examples include review notes, generated issue table, and memory tracking plans/docs. | Decide what belongs in the PR. Keep ADRs/tests; remove scratch plans before merge. | S | P2 |
| DBMEM-44 | Tests | Tests | Open | Tests should be mapped to the final issue list and allocator/thread decisions. | New tests exist, but coverage should follow final decisions. | Add focused tests for concurrent arena acquisition, background DB-aware threads, cross-thread deallocation, recovery attribution, tenant privilege/license failures, and replication behavior. | L | P1 |
