global:
  scrape_interval: 15s
  external_labels:
    cluster_id: "${CLUSTER_ID}"
    cluster_env: "${CLUSTER_ENV}"

scrape_configs:
  - job_name: memgraph-exporter
    static_configs:
      - targets:
          - mg-exporter:9115
