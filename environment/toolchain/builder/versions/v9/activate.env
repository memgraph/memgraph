# The activate stage places the activation script and pins no version of its
# own. The script works out the toolchain's location, name and version from
# where it finds itself, so this stage needs none of them.
