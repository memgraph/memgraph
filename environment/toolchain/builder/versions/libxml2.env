# Versions for the libxml2 stage. Bumping one here rebuilds this stage
# and everything downstream of it, and nothing else.
#
# In the sysroot for libabigail, which stores ABI corpora as XML.
LIBXML2_VERSION=2.15.3
LIBXML2_SHA256="78262a6e7ac170d6528ebfe2efccdf220191a5af6a6cd61ea4a9a9a5042c7a07"
