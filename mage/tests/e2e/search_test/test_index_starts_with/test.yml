query: >
  CALL search.node({Person: 'name'}, 'starts with', 'Bob') YIELD node
  RETURN node ORDER BY node.name;

output:
  - node: {"labels": ["Person"], "properties": {"name": "Bob"}}
  - node: {"labels": ["Person"], "properties": {"name": "Bobby"}}
