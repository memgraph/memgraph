query: >
  MATCH (dog:Dog)
  CALL path.subgraph_all(dog, {labelFilter: ["/Mouse"], filterStartNode: false})
  YIELD nodes, rels
  RETURN nodes, rels

output:
  - nodes:
      - labels:
          - Dog
        properties: {}
      - labels:
          - Mouse
        properties: {}
    rels:
      - label: FRIENDS_WITH
        properties: {}
