core:non_interactive=True

# Download resilience (useful for CI with flaky networks)
core.download:retry=3
core.download:retry_wait=5

# Parallel downloads to speed up dependency fetching
core.download:parallel=4
